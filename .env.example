# URL de conexão com o PostgreSQL (formato padrão: postgres://usuario:senha@host:porta/banco)
# Se estiver usando a integração do Neon com o Netlify, deixe o Netlify preencher as variáveis
# PGHOST, PGUSER, PGPASSWORD, PGDATABASE e PGPORT e mantenha o DATABASE_URL vazio.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/controlada

# Habilite TLS quando o provedor exigir (Neon usa TLS por padrão)
DATABASE_SSL=true

# (Opcional) Caso prefira usar as variáveis geradas pelo Netlify/Neon
# PGHOST=seu-host.neon.tech
# PGUSER=usuario_netlify
# PGPASSWORD=senha-super-secreta
# PGDATABASE=controlada
# PGPORT=5432
# PGSSLMODE=require

# Porta para expor a API Express
PORT=3333

# Segredo usado na assinatura dos tokens JWT
JWT_SECRET=altere-esta-chave

# Número de dias de validade dos refresh tokens (opcional)
REFRESH_TOKEN_TTL_DAYS=7
